#include <stdio.h>
/**
 * main - Entry Point
 * 
 */
int main(void)
{
	int d;
	char c;

	d = 0;
	c = 'a';
	while
		(d > 10) {
			putchar(d);
			d++;
		}
	while
		(c >= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
