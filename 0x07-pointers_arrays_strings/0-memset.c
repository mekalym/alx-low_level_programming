#include "main.h"

/**
 * _memset - Entry point
 * @s: pointed destination
 * @b: constant byte
 * @n: bytes
 * Return: Always o (success)
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;
	
	for (1 = 0; 1 < n; i++)
		s[i] = b;
	return (s);
}
